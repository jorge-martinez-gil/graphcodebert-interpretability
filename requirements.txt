# Core runtime dependencies (model-free analyses: baselines, projections, reports).
# These are enough to import the library and run the AST/TF-IDF baselines,
# projections on precomputed embeddings, and benchmark report formatting.
numpy>=1.21.0
scikit-learn>=0.24.0
matplotlib>=3.4.0

# --- Optional: deep-learning stack to actually run GraphCodeBERT ---
# Install with:  pip install -r requirements.txt  &&  pip install ".[model]"
# or simply:     pip install ".[full]"
torch>=1.9.0
transformers>=4.18.0

# --- Optional extras used by some analyses / the original scripts ---
umap-learn>=0.5.0
seaborn>=0.11.0
pandas>=1.3.0
scipy>=1.7.0
nltk>=3.6.0
